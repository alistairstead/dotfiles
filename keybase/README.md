# Keybase install and configuration

The `install.sh` will create config files and add configuration for
the gpgagent.

To import keys from [Keybase](https://keybase.io/) you will need to run:

```
~/.dotfiles/keybase/import_keys.sh
```
